#include "Document.h"
#pragma once

class Editor
{
    //data member
    Document _doc; //using the Document class for all actions needed

public:
    //only function - which used to work with the editor
    void loop();
};